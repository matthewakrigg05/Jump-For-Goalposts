package gui.admin;
import javax.swing.*;
import leagueDB.matchData;
import leagueDB.teamData;

public class genFixturesDialog extends JDialog implements matchData, teamData {

	public genFixturesDialog() { initialise(); }
	
	public void initialise() {
		
		// need: season drop down -> most recent first, add teams to season, gen fixtures button
		
	}
}
